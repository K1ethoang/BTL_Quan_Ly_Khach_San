#include "RoomRenters.hpp"
#include "menu.hpp"

int main()
{
    menu();
    // // FILE *fileIn = fopen("../File/roomRenter/roomRenter.in", "r");
    // // FILE *fileOut = fopen("../File/roomRenter/roomRenter.out", "w");
    // RoomRenters r;
    // createList(r);
    // Room rooms[20];
    // int n;
    // readRooms(rooms, n);
    // // outputRooms(rooms, n);
    // readRoomRenters(r, rooms, n);
    // outputRoomRenters(r);
    // writeRoomRenters(r);
    // // fclose(fileIn);
    // // fclose(fileOut);
    return 0;
}